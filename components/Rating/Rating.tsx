import { iRating } from './Rating.props';

const Rating = ({isChange = false, rating, setRating, ...props}: iRating) => {
    return (
        <>
        {}
        </>

    );
};

export default Rating;